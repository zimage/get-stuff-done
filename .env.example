DATABASE_URL=postgresql://gsd:gsd@localhost:5432/gsd
JWT_SECRET=dev-secret-change-me
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
# Not used by the app yet (our sign-in flow only needs the client ID) — kept
# here for whenever a feature needs server-side Google API access.
GOOGLE_CLIENT_SECRET=
